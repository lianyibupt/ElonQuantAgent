# 账户分析使用说明

本文档说明当前项目中“账户分析（Account Analysis）/ Stage 2 工作区”的使用方式，包括入口、数据保存位置、必须填写的信息、推荐操作流程，以及结果查看方式。

## 1. 功能定位

账户分析不是单只股票分析的替代，而是在现有单票分析能力之上，增加一层面向真实账户管理的工作区。

你可以在网页里维护：
- 账户总资产与现金
- 持仓清单
- 候选股票清单
- 账户分析历史结果

系统会基于这些内容做两类工作：
1. 在单只股票分析后，尝试把最新价格和相关摘要回写到已保存持仓中
2. 在账户分析中，批量刷新持仓价格，并生成账户级 AI 分析结论

---

## 2. 数据保存在哪里

当前账户分析数据会保存在本地 SQLite 数据库中：

- 数据库文件：`trading_data.db`
- 工作区表：`stage2_workspaces`

保存方式：
- 以 `workspace_name` 区分工作区
- 默认使用 `default`
- 账户、持仓、候选池、备注等内容整体以 JSON 形式保存在 `workspace_data` 字段中

这意味着：
- 刷新页面后，已保存的数据仍然存在
- 重启服务后，已保存的数据仍然存在
- 只要不删除本地数据库文件，工作区内容会一直保留

### 账户分析结果文件

除了数据库中的工作区快照，账户分析每次运行后还会额外生成结果文件，保存在：

- `artifacts/account_analysis/<workspace_name>/`

默认工作区通常会在这里看到：

- `artifacts/account_analysis/default/`

每次分析会生成两份文件：
- `时间戳.json`
- `时间戳.md`

系统默认保留最近 10 次账户分析记录，旧记录会自动清理。

---

## 3. 如何进入账户分析页面

启动项目后：

```bash
python run.py
```

然后打开浏览器访问页面。

在主页面中：
- 默认是单股票分析相关区域
- 切换到 `Account Analysis` 页签，即可进入账户分析工作区

在这个页签中，你会看到：
- 账户信息区域
- 持仓编辑区域
- 候选池区域
- 保存/加载相关按钮
- `Run Account Analysis` 按钮
- 历史分析记录列表

---

## 4. 你必须手动填写哪些信息

当前实现里，真正需要你手工维护的字段分为两层：账户层和持仓层。

### 4.1 账户层必须填写

建议至少填写：

- `nav`：账户总资产
- `cash`：当前现金

这两个字段是账户分析最核心的输入。

### 4.2 账户层建议填写

- `current_drawdown`：当前回撤

这个字段不是绝对硬性必填，但建议维护，因为它会让账户风险状态更接近你的真实交易情况。

### 4.3 每只持仓必须填写

建议最少填写以下 4 项：

- `ticker`：股票代码
- `cost_basis`：持仓成本价
- `shares`：持股数量
- `book_type`：仓位类型

其中：
- `ticker` 用于识别持仓与拉取最新价格
- `cost_basis` 和 `shares` 用于计算市值、浮盈亏、收益率
- `book_type` 建议填写为 `core` 或 `tactical`，便于区分核心仓和战术仓

### 4.4 每只持仓建议补充填写

这些不是最小必填，但建议逐步维护：

- `tracking_status`：当前跟踪状态
- `factor_tags`：风格/因子标签
- `notes`：备注

适合用来表达：
- 观察中 / 持有中 / 减仓观察 / 计划退出
- AI、加密、趋势、反转、财报驱动等标签
- 个股的主逻辑、风险点、操作纪律

---

## 5. 哪些字段不需要你手动填写

以下字段是系统自动计算或自动刷新出来的，你不需要手工维护：

- `latest_price`
- `market_value`
- `position_weight`
- `unrealized_pnl`
- `unrealized_pnl_pct`
- `price_source`
- `last_price_update_at`
- `last_analyzed_at`
- `last_analysis_summary`

账户层的这些字段也属于系统派生：

- `total_market_value`
- `cash_pct`
- `gross_exposure`
- `core_exposure`
- `tactical_exposure`
- `largest_position`
- `largest_position_weight`
- `position_count`
- `last_aggregated_at`

也就是说，系统设计原则是：
- 你负责维护“真实输入”
- 系统负责推导“计算结果”和“分析结果”

---

## 6. 推荐的填写顺序

如果你第一次使用，建议按下面顺序录入。

### 第一步：先填账户信息

先填写：
- `nav`
- `cash`
- `current_drawdown`（建议）

### 第二步：录入已有持仓

每个持仓至少填：
- `ticker`
- `cost_basis`
- `shares`
- `book_type`

建议再补：
- `tracking_status`
- `factor_tags`
- `notes`

### 第三步：补充候选池

候选池主要用于记录你正在观察、还未正式持有的标的。

建议填写：
- `ticker`
- `factor_tags`
- `notes`

### 第四步：保存工作区

录入完成后，点击保存工作区按钮，把当前内容写入本地数据库。

建议养成习惯：
- 每次做完较大修改就保存一次
- 每次新开页面后先加载一次当前工作区

---

## 7. 如何运行账户分析

当工作区已经录入并保存后，可以直接点击：

- `Run Account Analysis`

运行时系统会做这些事：

1. 读取当前工作区（默认 `default`）
2. 为每个持仓尝试刷新最新价格
3. 根据最新价格重新计算：
   - 市值
   - 仓位占比
   - 浮盈亏
   - 浮盈亏比例
4. 生成账户级 AI 分析
5. 把本次分析结果保存到历史目录
6. 在页面中展示本次分析结果与历史记录

如果某个持仓没有有效 ticker，或者价格拉取失败，系统会跳过该持仓，并在返回结果中记录跳过原因。

---

## 8. 单股票分析和账户分析如何联动

当前系统已经支持一个很实用的联动：

### 单股票分析后的持仓回写

如果你在单股票分析里分析了一只股票，并且这只股票已经存在于已保存的账户持仓中，系统会尝试自动回写：

- 最新价格
- 最新分析时间
- 分析摘要
- 重新计算后的浮盈亏、仓位权重等

这意味着你可以：
- 先维护好持仓底仓信息（成本价、股数）
- 再通过单票分析不断刷新重点持仓
- 最后在账户分析中看整体组合状态

这是当前版本里最适合日常使用的方式。

---

## 9. 账户分析结果怎么看

每次运行账户分析后，你会得到两类结果：

### 9.1 页面内结果

页面会展示本次账户分析的结果，包括：
- 总结摘要
- 持仓健康度
- 风险集中点
- 操作建议
- 历史分析记录

### 9.2 落盘结果文件

每次运行都会额外生成：
- `.json`：结构化分析结果
- `.md`：可直接阅读的分析报告

保存目录：

```text
artifacts/account_analysis/default/
```

如果以后你增加多个工作区，则会变成：

```text
artifacts/account_analysis/<workspace_name>/
```

这些文件适合：
- 回顾过去 10 次账户判断
- 对比不同时间点的组合变化
- 后续做归档或人工复盘

---

## 10. 推荐的日常使用流程

如果你是实盘在用，建议采用下面流程：

### 日常维护

1. 打开网页
2. 进入 `Account Analysis`
3. 更新账户信息
4. 更新持仓数量、成本和状态
5. 保存工作区

### 盘中/盘后分析

1. 对重点持仓先做单股票分析
2. 让系统尽量把最新价格和摘要回写到持仓里
3. 再运行一次账户分析
4. 查看整体风险、仓位、集中度和 AI 操作建议

### 周度复盘

1. 打开历史分析记录
2. 对比最近几次账户分析结果
3. 观察：
   - 风险是否上升
   - 哪些仓位持续恶化
   - 哪些建议反复出现
   - 核心仓/战术仓结构是否失衡

---

## 11. 当前版本的最小可用填写模板

如果你想尽快先跑起来，可以按这个最小模板录入。

### 账户

- `nav` = 账户总资产
- `cash` = 当前现金

### 持仓示例

#### 持仓 1
- `ticker` = AAPL
- `cost_basis` = 180
- `shares` = 100
- `book_type` = core

#### 持仓 2
- `ticker` = TSLA
- `cost_basis` = 210
- `shares` = 30
- `book_type` = tactical

这个最小集合就足够让系统开始：
- 刷新价格
- 计算盈亏
- 生成账户分析

---

## 12. 常见问题

### 12.1 不填写 `book_type` 可以吗？

可以运行，但不建议。

因为账户分析里很重要的一部分是区分：
- 核心仓（core）
- 战术仓（tactical）

如果不填，系统对组合结构的理解会变弱。

### 12.2 不填写 `current_drawdown` 可以吗？

可以。

但如果你非常重视回撤管理，建议维护这个字段，它有助于让账户分析更贴近你的真实风险状态。

### 12.3 候选池是必须的吗？

不是必须。

如果你当前只想先管理现有持仓，可以先不填候选池。

### 12.4 为什么有些价格没有刷新成功？

常见原因包括：
- ticker 填写不规范
- 数据源暂时失败
- 当前时间窗口里拿不到有效收盘价

建议优先检查 ticker 是否规范。

### 12.5 我修改了页面内容，但刷新后没了，为什么？

通常是因为没有点保存。

页面上的临时修改只有在保存工作区后，才会写入本地数据库。

---

## 13. 你当前最应该维护的字段

如果只从“真实交易最有价值”的角度出发，建议你优先长期维护这些字段：

### 账户
- `nav`
- `cash`
- `current_drawdown`

### 持仓
- `ticker`
- `cost_basis`
- `shares`
- `book_type`
- `tracking_status`
- `factor_tags`
- `notes`

这套字段已经足够支撑：
- 账户风险视角
- 仓位结构视角
- 盈亏跟踪视角
- AI 组合建议视角

---

## 14. 后续建议

当前版本已经可以完成本地账户分析闭环，但后续仍然值得继续优化：

- 把持仓输入区从紧凑表格改成更易填写的卡片式布局
- 明确区分“必须填写”和“系统自动计算”字段
- 增加更清晰的账户分析摘要面板
- 增加更适合实盘的风险提示与仓位建议展示

如果你愿意，下一步我可以继续帮你补两份文档：

1. `账户分析快速上手.md`：更短、更像操作清单
2. `账户分析字段说明.md`：按字段逐项解释含义与建议填写方式
